rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for property images
    match /propertyImages/{allPaths=**} {
      allow read;
    }
    // Authenticated users can write anywhere
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}
